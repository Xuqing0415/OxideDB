# Posts

* `readindex-zh.md` - the canonical version: full Markdown, repository links intact.
* `readindex-zh-csdn.md` - the same article plus a publishing header (title, summary,
  tags, category) for CSDN, which renders Markdown as-is.
* `readindex-zh-wechat.md` - adapted for a WeChat official account: the timeline is a
  list so it survives a phone screen, and the repository URL is plain text because
  WeChat articles cannot carry external links.
* `prototype-to-product-zh.md` - the canonical version of the second article: full
  Markdown, file paths and commit hashes intact.
* `prototype-to-product-zh-csdn.md` - the same article plus a publishing header (title,
  alternate title, summary, tags, category) for CSDN, which renders Markdown as-is.
* `prototype-to-product-zh-wechat.md` - the same article plus a publishing note for a
  WeChat official account.  Nothing had to be adapted this time: the article carries no
  aligned figures and no external links, so every line below the note is the canonical
  version's line, byte for byte.